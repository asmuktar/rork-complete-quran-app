# Expo environment variables
# Set this to your backend URL when available
# EXPO_PUBLIC_RORK_API_BASE_URL=https://your-backend-url.com

# For now, the app will use local data only
# This prevents network errors when backend is not available